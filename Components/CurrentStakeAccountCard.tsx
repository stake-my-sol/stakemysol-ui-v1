import { Paper, Typography } from "@mui/material";

interface CurrentStakeAccountCardProps {}

function CurrentStakeAccountCard({}: CurrentStakeAccountCardProps) {
  return (
    <Paper elevation={3}>
      <Typography>Current Stake Account Card</Typography>
    </Paper>
  );
}

export default CurrentStakeAccountCard;
